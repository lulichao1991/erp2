import { purchaseMock, purchasesMock } from '@/mocks/purchases'

export const transactionMock = purchaseMock
export const transactionsMock = purchasesMock

// Compatibility export for existing imports.
export const mockTransactionRecords = transactionsMock
