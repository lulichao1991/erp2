下面我直接继续给你一份可放进仓库的 **`docs/frontend/codex-prompts-phase-2.md`**。
这份就是给 Codex / AI coding 用的分阶段执行 prompt 清单，尽量按你现在仓库的结构、节奏和约束来写。

```md
# Codex Prompts Phase 2（给 AI Coding 的第二轮执行提示清单）

## 1. 文档定位

本文档用于指导 AI coding 工具在当前仓库基础上，逐步完成 ERP Phase 2 的前端实现。

本文件不重复定义产品需求，而是把需求拆成适合 AI 执行的阶段化 prompt。

配套文档：

- `phase-2-prd.md`
- `order-lifecycle-spec.md`
- `task-center-prd.md`
- `role-draft.md`
- `phase-2-task-board.md`

---

## 2. 使用原则

### 原则 1：一次只做一个阶段
不要把整个 Phase 2 一次性交给 AI。

### 原则 2：先读仓库现状，再修改
每次执行前，先让 AI 阅读当前相关目录、页面、类型、mock 数据和 store，再开始改。

### 原则 3：先保证不破坏现有功能
Phase 2 是增量升级，不是重写 Phase 1。

### 原则 4：优先保留现有目录风格
新代码尽量延续当前仓库的：
- 路由组织方式
- 页面目录风格
- types 分层
- utils 纯函数分层
- docs/frontend 文档口径

### 原则 5：先做结构与联动，再做视觉优化
优先级是：
- 数据结构
- 页面结构
- 页面联动
- 基础交互
- 样式细节

---

## 3. AI 执行总顺序

建议总顺序如下：

1. Phase 2-01：类型与 mock 数据升级
2. Phase 2-02：订单中心详情页升级
3. Phase 2-03：订单状态流转与时间线
4. Phase 2-04：任务中心接入
5. Phase 2-05：从订单创建任务并联动
6. Phase 2-06：产品档案升级
7. Phase 2-07：首页增强
8. Phase 2-08：角色模式与页面显隐
9. Phase 2-09：收尾、联调与清理

---

# 4. 分阶段 Prompt 清单

# Prompt 01：先做类型与 mock 数据升级

## 目标
先补齐 Phase 2 所需的数据结构，让后续页面开发有稳定基础。

## 交给 AI 的 Prompt

你现在要在当前仓库基础上，完成 ERP Phase 2 的第一步：类型与 mock 数据升级。

请先阅读并理解以下内容后再改代码：

- `docs/frontend/phase-2-prd.md`
- `docs/frontend/order-lifecycle-spec.md`
- `docs/frontend/task-center-prd.md`
- 当前 `src/types`
- 当前 mock 数据目录
- 当前订单、产品相关页面结构

本次任务只做“类型和 mock 数据层升级”，不要提前大改页面 UI。

请完成以下内容：

1. 为订单补充或调整类型：
   - `OrderStatus`
   - `Order`
   - `OrderItem`

2. 为任务新增类型：
   - `TaskType`
   - `TaskStatus`
   - `Task`
   - 如有必要的筛选类型

3. 为订单时间线新增类型：
   - `TimelineRecord`

4. 升级 mock 数据：
   - 订单新增 `status`、`ownerName`、`priority`、`dueDate`、`activeTaskCount`、`latestActivityAt`
   - 订单支持 `items[]`
   - 每个 `OrderItem` 包含来源产品快照、规格、参数、报价、人工调整字段
   - 新增任务 mock 数据
   - 新增时间线 mock 数据

5. 保持现有功能可运行，不破坏当前订单中心和产品管理页面

输出要求：
- 优先最小改动
- 保持类型命名清晰
- 如果发现现有类型命名不一致，请统一但不要过度重构
- 最后给出修改文件清单和后续建议

---

# Prompt 02：升级订单详情页结构

## 目标
让订单详情页成为 Phase 2 的主业务页面。

## 交给 AI 的 Prompt

你现在要在当前仓库基础上，升级订单详情页，使其符合 Phase 2 的结构要求。

请先阅读：

- `docs/frontend/phase-2-prd.md`
- `docs/frontend/order-lifecycle-spec.md`
- 当前订单详情页实现
- 当前订单相关 types、mock 数据、store（如有）

本次任务只聚焦订单详情页结构升级，不做任务中心独立页面，不做复杂角色逻辑。

请完成以下内容：

1. 升级订单详情页顶部基础信息区，展示：
   - 订单编号
   - 客户名称
   - 当前状态
   - 跟单负责人
   - 交期
   - 紧急程度

2. 增加“状态流转区”占位：
   - 显示当前状态
   - 显示可切换的下一状态入口
   - 本次可以先不完成全部状态逻辑，但结构要预留好

3. 增加“订单商品区”：
   - 支持展示多个订单商品
   - 每行显示来源产品、规格、参数、基础价、系统参考报价、人工调整、备注

4. 增加“任务摘要区”占位：
   - 展示当前订单关联任务的简要列表
   - 提供“新建任务”按钮占位
   - 提供跳转任务详情入口占位

5. 增加“时间线区”：
   - 展示时间线记录列表
   - 每条显示时间、操作人、事件文案

6. 页面布局尽量沿用现有风格，不要做大幅视觉重设计

输出要求：
- 不破坏现有订单详情基础能力
- 页面结构清晰、模块边界清楚
- 组件可适当拆分，但不要过度拆分
- 最后给出修改文件清单

---

# Prompt 03：实现订单状态流转与时间线联动

## 目标
让订单状态从展示变成可操作。

## 交给 AI 的 Prompt

你现在要在当前仓库基础上，实现 Phase 2 的订单状态流转基础逻辑，并与时间线联动。

请先阅读：

- `docs/frontend/order-lifecycle-spec.md`
- 当前订单详情页
- 当前订单类型定义
- 当前时间线 mock 数据或相关实现

本次任务只处理订单状态流转和时间线联动，不做任务中心独立页面。

请完成以下内容：

1. 基于以下状态实现前端可用的状态流转逻辑：
   - `draft`
   - `pending_confirm`
   - `pending_design`
   - `pending_production_prep`
   - `pending_shipping`
   - `after_sales`
   - `completed`
   - `cancelled`

2. 在订单详情页中：
   - 只展示当前状态允许切换到的下一状态
   - 不允许展示明显不合法的跳转

3. 状态切换后：
   - 更新订单状态
   - 更新时间
   - 写入新的时间线记录 `status_changed`

4. 若订单进入以下状态，给出建议操作提示（先做前端提示即可）：
   - 进入 `pending_design` 时提示建议创建设计建模任务
   - 进入 `pending_production_prep` 时提示建议创建生产准备任务
   - 进入 `after_sales` 时提示建议创建售后任务

5. 保持当前页面风格一致，不做复杂弹窗工作流

输出要求：
- 代码以当前项目风格为准
- 优先简单清晰
- 不提前引入后端接口抽象
- 最后说明哪些规则已实现，哪些仍是占位

---

# Prompt 04：新增任务中心路由与任务列表页

## 目标
让任务中心正式进入系统导航。

## 交给 AI 的 Prompt

你现在要在当前仓库中新增 ERP Phase 2 的任务中心基础结构。

请先阅读：

- `docs/frontend/task-center-prd.md`
- `docs/frontend/phase-2-prd.md`
- 当前路由配置
- 当前页面目录结构
- 当前导航组件实现

本次任务只做：
- 任务中心接入路由
- 任务列表页
不做任务详情页，不做从订单创建任务表单。

请完成以下内容：

1. 新增路由：
   - `/tasks`
   - `/tasks/:taskId`（本次可以先接占位页）

2. 更新系统导航，加入“任务中心”入口

3. 新建任务列表页 `/tasks`，页面需要展示：
   - 任务标题
   - 任务类型
   - 任务状态
   - 关联订单编号
   - 关联订单商品摘要（如有）
   - 责任角色
   - 责任人
   - 优先级
   - 截止时间
   - 是否逾期
   - 更新时间

4. 实现基础筛选：
   - 任务类型
   - 状态
   - 责任角色
   - 责任人
   - 是否逾期

5. 支持从任务列表点击进入任务详情占位页

输出要求：
- 页面风格与现有模块一致
- 优先复用现有表格、卡片、筛选区风格
- 任务列表页可先用 mock 数据驱动
- 最后给出修改文件清单

---

# Prompt 05：完成任务详情页

## 目标
让任务可独立查看与处理。

## 交给 AI 的 Prompt

你现在要在当前仓库中完成 ERP Phase 2 的任务详情页。

请先阅读：

- `docs/frontend/task-center-prd.md`
- 当前任务列表页实现
- 当前订单详情页实现
- 当前任务类型与 mock 数据

本次任务只聚焦任务详情页，不做从订单创建任务。

请完成以下内容：

1. 新建或完善任务详情页 `/tasks/:taskId`

2. 页面至少包含以下区域：

### 基础信息区
- 任务标题
- 任务类型
- 任务状态
- 优先级
- 截止时间
- 责任角色
- 责任人

### 来源信息区
- 来源订单编号
- 来源订单状态
- 客户名称
- 来源订单商品摘要（如有）

### 任务说明区
- 描述
- 备注
- 附件占位

### 操作区
- 修改任务状态
- 标记完成
- 关闭任务

3. 支持从任务详情页跳转回来源订单详情页

4. 任务状态更新后，先在本地 mock / store 中同步数据

输出要求：
- 页面结构清楚
- 信息层次明确
- 尽量与订单详情页风格统一
- 最后给出修改文件清单与后续建议

---

# Prompt 06：从订单详情页发起任务并完成联动

## 目标
把任务真正接进订单主链路。

## 交给 AI 的 Prompt

你现在要在当前仓库中实现“从订单详情页发起任务”，并打通订单与任务的基础联动。

请先阅读：

- `docs/frontend/task-center-prd.md`
- `docs/frontend/order-lifecycle-spec.md`
- 当前订单详情页
- 当前任务中心页面与任务数据结构

本次任务重点是联动，不要大改 UI 风格。

请完成以下内容：

1. 在订单详情页增加“新建任务”入口

2. 新建任务时支持填写：
   - 任务类型
   - 任务标题
   - 责任角色
   - 责任人
   - 优先级
   - 截止时间
   - 描述
   - 是否关联某个订单商品

3. 任务创建成功后：
   - 加入任务列表
   - 出现在订单详情页任务摘要区
   - 更新订单 `activeTaskCount`
   - 写入订单时间线 `task_created`

4. 任务完成后：
   - 更新任务状态
   - 更新订单 `activeTaskCount`
   - 写入订单时间线 `task_completed`

5. 若任务关联某个订单商品，请在订单商品行中展示关联任务数量或相关提示

输出要求：
- 优先简单可用
- 避免复杂弹窗嵌套
- 保持订单页、任务页、时间线三者一致
- 最后给出修改文件清单与测试建议

---

# Prompt 07：升级产品详情页与产品编辑页

## 目标
把产品管理升级为“标准产品档案”。

## 交给 AI 的 Prompt

你现在要在当前仓库中升级产品详情页和产品编辑页，使其符合 Phase 2 的产品档案结构。

请先阅读：

- `docs/frontend/phase-2-prd.md`
- 当前产品列表页、详情页、编辑页
- 当前产品类型与 mock 数据

本次任务只做产品详情与编辑增强，不做任务中心改动。

请完成以下内容：

1. 升级产品详情页结构，增加以下模块：
   - 基础信息
   - 规格与参数
   - 价格与加价规则
   - 图片与文件
   - 业务说明

2. 产品详情页建议展示字段包括：
   - 产品编码
   - 产品名称
   - 品类
   - 系列
   - 标签
   - 状态
   - 默认材质
   - 可选材质
   - 可选工艺
   - 特殊需求标签
   - 价格说明
   - 适用渠道
   - 生产注意事项
   - 常见售后问题
   - 内部备注

3. 升级产品编辑页 / 新建页，支持编辑以上新增字段中的合理部分

4. 保持现有“规格 + 固定加价规则”主能力不被破坏

输出要求：
- 先保证信息结构完整
- 图片 / 文件先做占位，不做真实上传系统
- 页面风格保持统一
- 最后给出修改文件清单

---

# Prompt 08：首页增强为轻量工作台

## 目标
让首页不再只是入口页，而是具备轻量摘要能力。

## 交给 AI 的 Prompt

你现在要在当前仓库中升级首页，使其符合 ERP Phase 2 的轻量工作台定位。

请先阅读：

- `docs/frontend/phase-2-prd.md`
- 当前首页实现
- 当前订单、任务相关 mock 数据

本次任务只做首页增强，不做复杂 BI 看板。

请完成以下内容：

1. 在首页新增摘要卡片：
   - 待处理订单数
   - 今日到期任务数
   - 逾期任务数
   - 最近更新订单数

2. 在首页新增快捷入口：
   - 新建订单
   - 查看待确认订单
   - 查看待设计任务
   - 查看逾期任务
   - 查看产品列表

3. 保持首页简洁，不要做过于复杂的统计图表

4. 首页信息来源先使用现有 mock 数据和前端计算逻辑

输出要求：
- 首页仍应保持轻量
- 卡片与快捷入口层级清楚
- 与现有系统视觉保持一致
- 最后给出修改文件清单

---

# Prompt 09：接入角色模式与页面显隐

## 目标
先用轻量方式模拟角色差异。

## 交给 AI 的 Prompt

你现在要在当前仓库中接入 Phase 2 的角色模式，用轻量方式实现页面和动作的基础显隐。

请先阅读：

- `docs/frontend/role-draft.md`
- 当前路由与导航实现
- 当前订单、产品、任务页面结构

本次任务不做正式权限系统，只做前端角色模式模拟。

请完成以下内容：

1. 在 mock 用户信息中加入角色字段，建议包括：
   - `customer_service`
   - `designer`
   - `operator`
   - `management`

2. 在路由、导航或页面入口层面，做基础显隐：
   - 设计建模默认不显示“新建订单”
   - 管理层默认不显示“产品编辑”
   - 跟单/运营可以看到更多管理动作
   - 客服可以看到订单和基础任务入口

3. 在关键页面中做简单操作级显隐 / 禁用：
   - 状态切换按钮
   - 报价调整区域
   - 产品核心字段编辑
   - 任务责任人修改

4. 首页摘要内容可根据角色略有不同，但不要过度复杂化

输出要求：
- 不要搭完整权限框架
- 优先做轻量前端模拟
- 代码结构清楚，后续方便接真正权限系统
- 最后给出修改文件清单与局限说明

---

# Prompt 10：收尾、联调、清理与文案统一

## 目标
把 Phase 2 收到一个可演示、可继续迭代的状态。

## 交给 AI 的 Prompt

你现在要对当前仓库的 ERP Phase 2 前端实现做一次收尾整理，目标是让“产品中心 → 订单中心 → 任务中心”主链路更稳定、更一致。

请先阅读：

- `docs/frontend/phase-2-prd.md`
- `docs/frontend/order-lifecycle-spec.md`
- `docs/frontend/task-center-prd.md`
- `docs/frontend/role-draft.md`
- 当前所有 Phase 2 已改过的页面、types、mock、store

请完成以下内容：

1. 检查并修复以下链路：
   - 从产品引用到订单商品
   - 从订单创建任务
   - 任务显示在订单详情页
   - 任务完成写回订单摘要
   - 时间线能正确显示关键动作
   - 首页摘要能正确读取订单 / 任务信息

2. 统一术语文案：
   - 订单状态文案
   - 任务状态文案
   - 任务类型文案
   - 角色文案
   - 页面标题与按钮文案

3. 清理明显重复、无效或临时性的代码片段，但不要做激进重构

4. 输出一份简短总结：
   - 已完成的能力
   - 已知限制
   - 建议下一步

输出要求：
- 以稳定为先
- 以一致性为先
- 避免为了“更漂亮”做大规模样式重写
- 最后给出修改文件清单与回归检查点

---

## 5. 推荐执行节奏

建议你实际使用时按下面顺序推进，而不是跳着做：

### 第一轮
- Prompt 01
- Prompt 02

### 第二轮
- Prompt 03

### 第三轮
- Prompt 04
- Prompt 05

### 第四轮
- Prompt 06

### 第五轮
- Prompt 07
- Prompt 08

### 第六轮
- Prompt 09
- Prompt 10

---

## 6. 每轮执行后的检查方式

每执行完一个 prompt，建议都做一次小检查。

### 检查 1：功能是否能跑通
- 页面是否能打开
- 基础跳转是否正常
- mock 数据是否报错

### 检查 2：类型是否一致
- 是否新增了重复类型
- 是否出现 any 乱飞
- 是否命名不统一

### 检查 3：是否破坏旧功能
- 产品列表 / 详情还能不能看
- 订单列表 / 详情还能不能看
- 报价逻辑是否被误改

### 检查 4：页面术语是否一致
- 状态文案是否统一
- 角色文案是否统一
- 按钮命名是否清楚

---

## 7. 本文档总结

这份文档的目的，不是让 AI 一口气生成完整 ERP，而是让它：

**一阶段一阶段，稳定地把当前仓库从 Phase 1 推到 Phase 2。**

核心顺序始终不变：

**先类型和 mock → 再订单 → 再任务 → 再产品 → 再首页 → 再角色 → 最后联调收尾。**
```

到这里，你这套第二轮文档链已经比较完整了：

- `phase-2-prd.md`
- `order-lifecycle-spec.md`
- `task-center-prd.md`
- `role-draft.md`
- `phase-2-task-board.md`
- `codex-prompts-phase-2.md`

再往下，最合适的不是继续加文档，而是做两件事里的一个：

第一，帮你整理成一份 **README / docs 索引更新稿**，把这些新文档挂到仓库阅读顺序里。
第二，继续给你出 **“Phase 2 的前端页面清单 + 每页设计重点”**，更适合直接给 UI 或前端。